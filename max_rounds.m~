clc, clear all, format compact
gameId = 1:10000;
maxRounds  = min(160, ceil(gameId / 160.0) * 20);

figure(1)
clf(1)
hold all
stair(gameId, maxRounds)
stair(Ga
title("Number of turns per game")
xlabel("Game number")
ylabel("Number of turns")

firstFullLengthRound =min(find(maxRounds==max(max(maxRounds))))